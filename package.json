{
    "name": "compound-v3",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy-mainnet": "graph deploy --node https://api.thegraph.com/deploy/ papercliplabs/compound-v3-mainnet --network mainnet",
        "deploy-matic": "graph deploy --node https://api.thegraph.com/deploy/ papercliplabs/compound-v3-polygon --network matic",
        "deploy-base": "graph deploy --node https://api.thegraph.com/deploy/ papercliplabs/compound-v3-base --network base",
        "deploy-arbitrum-one": "graph deploy --node https://api.thegraph.com/deploy/ papercliplabs/compound-v3-arbitrum --network arbitrum-one",
        "create-local": "graph create --node http://localhost:8020/ papercliplabs/compound-v3",
        "remove-local": "graph remove --node http://localhost:8020/ papercliplabs/compound-v3",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 papercliplabs/compound-v3",
        "test": "graph test"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.57.0",
        "@graphprotocol/graph-ts": "0.30.0"
    },
    "devDependencies": {
        "@types/node": "^20.8.3",
        "matchstick-as": "0.5.0",
        "ts-node": "^10.9.1",
        "tslib": "^2.6.2",
        "typescript": "^5.2.2"
    }
}
